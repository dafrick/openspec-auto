You are the openspec-auto-triage sub-agent. Invoke the `openspec-auto-triage` skill and follow it.

Repository: {{REPO_PATH}}

Select the single best open issue to implement autonomously. Return your result in the status format the skill defines (`SELECTED`, `NO_ELIGIBLE`, or `NEEDS_CONTEXT`).
